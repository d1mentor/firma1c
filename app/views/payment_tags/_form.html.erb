<%= form_with(model: payment_tag) do |form| %>
  <% if payment_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment_tag.errors.count, "error") %> prohibited this payment_tag from being saved:</h2>

      <ul>
        <% payment_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Тэг", style: "display: block" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "Комментарий к тэгу", style: "display: block" %>
    <%= form.text_field :comment, class: "form-control" %>
  </div>

  <div class="mt-2">
    <%= form.submit "Сохранить" , class: "btn btn-success" %>
  </div>
<% end %>
