<%= form_with(model: payment) do |form| %>
  <% if payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
        <% payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <strong>*Положительное число в сумме - поступление в бюджет</strong> <br>
  <strong>*Отрицательное число в сумме - Вычет из бюджета</strong>

  <div>
    <%= form.label :Дата, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.label :Сумма, style: "display: block" %>
    <%= form.number_field :size %>
  </div>

  <div>
    <%= form.label :Комментарий, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <%= form.hidden_field :source_type, value: params[:source_type] %>
  <%= form.hidden_field :source_id, value: params[:source_id] %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
